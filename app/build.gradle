plugins {
    id 'com.android.application'
}

android {
    namespace = 'me.s1204.benesse.dcha.test'
    compileSdk = 35

    defaultConfig {
        minSdk = 22
        targetSdk = 35
        versionCode = 14
        versionName = "1.5.1"
        proguardFiles += 'proguard-rules.pro'
        multiDexEnabled = false
    }

    signingConfigs {
        android {
            storeFile file('android.jks')
            storePassword 'android'
            keyAlias 'android'
            keyPassword 'android'
        }
    }

    buildTypes {
        configureEach {
            signingConfig = signingConfigs.android
        }
        release {
            minifyEnabled = true
            shrinkResources = true
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_21
        targetCompatibility JavaVersion.VERSION_21
    }

    buildFeatures {
        buildConfig = false
    }

    lintOptions {
        abortOnError = false
        disable 'HardcodedText', 'InefficientWeight', 'Autofill', 'TextFields', 'Deprecated', 'TooManyViews', 'DiscouragedApi'
    }
}

dependencies {
    implementation files('libs/DchaService.jar')
    implementation files('libs/DchaUtilService.jar')
}
